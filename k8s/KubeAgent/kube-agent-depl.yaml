apiVersion: apps/v1
kind: Deployment
metadata:
  name: kube-agent
  labels:
    app: kube-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kube-agent
  template:
    metadata:
      name: kube-agent
      labels:
        app: kube-agent
    spec:
      containers:
        - name: kube-agent
          image: ghcr.io/icykcypher/kubelab/kube-agent:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
              protocol: TCP
      restartPolicy: Always